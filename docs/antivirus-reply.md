# Ответ на письмо Фунтиковой П.

---

**От:** Чага Валерий
**Кому:** Фунтикова Полина
**Тема:** Re: Исключения антивируса — AIS Release Preparation

---

Добрый день, Полина!

Благодарю за разъяснение. Полностью согласен, что исключение целого каталога — избыточное решение. Предлагаю более точечный подход.

### Конкретный файл для анализа

Основной файл, вызывающий срабатывание — **`Hide-ConsoleWindow.ps1`**:

```powershell
# Скрипт скрытия вспомогательной консоли (19 строк)
$psi = New-Object Diagnostics.ProcessStartInfo
$psi.FileName = "powershell.exe"
$psi.Arguments = "-NoProfile -WindowStyle Hidden -ExecutionPolicy Bypass -STA -File ..."
$psi.UseShellExecute = $false
$psi.CreateNoWindow = $true
[Diagnostics.Process]::Start($psi) | Out-Null
```

**Почему срабатывает:** `Start-Process -WindowStyle Hidden` + `CreateNoWindow = true` — эвристика классифицирует как "скрытый запуск процесса".

**Что делает скрипт:** скрывает вспомогательную консоль при запуске GUI-приложения. Пользователь видит только графическое окно, консоль работает в фоне.

**Файл для передачи в Kaspersky:** `C:\AIS\AI\Prod\scripts\Hide-ConsoleWindow.ps1`

### Что я уже сделал для снижения рисков

Ранее в проекте использовался `user32.dll` P/Invoke (`ShowWindow`) — он вызывал срабатывания. Я **уже заменил** его на управляемый .NET-аналог. Текущая реализация максимально безопасна, но эвристика всё равно реагирует на скрытый запуск.

### Конкретная просьба об исключениях

Прошу рассмотреть **targeted-исключения** для следующих файлов:

| Файл | Тип | Назначение |
|------|-----|-----------|
| `C:\AIS\AI\Prod\scripts\Hide-ConsoleWindow.ps1` | .ps1 | Скрытие консоли |
| `C:\AIS\AI\Prod\bin\Prod-GUI.ps1` | .ps1 | GUI-приложение (4600+ строк) |
| `C:\AIS\AI\Prod\tools\pbldump-1.3.1stable\PblDump.exe` | .exe | Экспорт объектов PowerBuilder |
| `C:\Program Files (x86)\Microsoft Visual SourceSafe\ss.exe` | .exe | Контроль версий VSS |

**Не прошу** исключения дляBAT-файлов и всего каталога `scripts/`.

### Контекст проекта

Проект AIS Release Preparation — внутренний инструмент для подготовки релизов. Используется для:
- Сравнения SQL-объектов между тестовой и боевой БД
- Выгрузки объектов PowerBuilder
- Интеграции с Jira/RFC
- Контроля версий (VSS)

Проект работает только на рабочей станции разработчика, не распространяется.

---

Буду благодарен за возможность передать `Hide-ConsoleWindow.ps1` на анализ в Kaspersky. Готов предоставить любую дополнительную информацию.

С уважением,
Чага Валерий
