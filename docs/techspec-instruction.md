# Инструкция: Создание технических спецификаций в формате Markdown

## 1. Обзор

Техническая спецификация (techspec) — документ, описывающий что, зачем и как будет реализовано в задаче. Формат Markdown обеспечивает:
- Версионирование через Git
- Конвертацию в HTML/PDF
- Читаемость как человеком, так и AI-агентами

---

## 2. Структура документа

### 2.1 Шапка (обязательно)

```markdown
# Техническая спецификация: [Название задачи]

| Поле | Значение |
|------|----------|
| **Задача** | SYBASE-XXXX / BSI-XXXX |
| **Статус** | Черновик / Утверждён / В работе / Завершено |
| **Дата** | ДД.ММ.ГГГГ |
| **Платформа** | AIS (Sybase + PowerBuilder) |
| **Автор** | ФИО |
```

### 2.2 Секции (порядок обязателен)

| # | Секция | Назначение |
|---|--------|------------|
| 1 | КОНТЕКСТ | Проблема, связи, источники |
| 2 | AS-IS | Текущее состояние (объекты, данные, логика) |
| 3 | TO-BE | Целевое состояние (блоки, DDL, код) |
| 4 | ЗАТРОНУТЫЕ СИСТЕМЫ | Влияние на другие системы |
| 5 | РИСКИ | Риски и снижение |
| 6 | КРИТЕРИИ ПРИЁМКИ | Продуктовые (AC) и технические (TAC) |
| 7 | ДЕКОМПОЗИЦИЯ | US и подзадачи |
| 8 | ПРИЛОЖЕНИЯ | DDL, код, скриншоты, TQ |

---

## 3. Правила оформления

### 3.1 Заголовки

```markdown
# Заголовок 1 — название задачи
## Заголовок 2 — секция (КОНТЕКСТ, AS-IS, TO-BE)
### Заголовок 3 — подсекция (Блок 1, Объекты, Данные)
#### Заголовок 4 — элемент (Таблица, DDL, Код)
```

**Правило:** Не более 4 уровней вложенности.

### 3.2 Таблицы

```markdown
| Колонка 1 | Колонка 2 | Колонка 3 |
|-----------|-----------|-----------|
| Значение 1 | Значение 2 | Значение 3 |
```

**Правило:** Всегда указывайте заголовки таблицы.

### 3.3 Код

**Однострочный код:**
```markdown
`имя_объекта`
```

**Блок кода (SQL):**
````markdown
```sql
-- SQL-код
SELECT * FROM table
```
````

**Блок кода (PowerShell/PB):**
````markdown
```powershell
// Псевдокод PB
dw_list.Retrieve()
```
````

**Блок кода (DDL):**
````markdown
```sql
-- DDL
ALTER TABLE table_name ADD column_name type
```
````

### 3.4 Списки

**Маркированный список:**
```markdown
- Элемент 1
- Элемент 2
- Элемент 3
```

**Нумерованный список:**
```markdown
1. Шаг 1
2. Шаг 2
3. Шаг 3
```

**Чек-лист:**
```markdown
- [ ] Задача не начата
- [x] Задача выполнена
```

### 3.5 Ссылки

```markdown
[Текст ссылки](URL)
```

### 3.6 Выделение

```markdown
**жирный текст**
*курсив*
`код`
```

---

## 4. Заполнение секций

### 4.1 КОНТЕКСТ

```markdown
## 1. КОНТЕКСТ

### 1.1 Проблема/запрос
[Опишите проблему или запрос, который решает данная задача]

### 1.2 Связи
- Связанные задачи: SYBASE-XXXX, BSI-XXXX
- Затронутые продукты: AIS, ЛК партнёра, ЭДО
- Владелец продукта: ФИО
- Технический владелец: ФИО

### 1.3 Источники
- Продуктовая спека: ссылка
- Wiki: ссылка
- Документация: ссылки
```

### 4.2 AS-IS

```markdown
## 2. AS-IS (текущее состояние)

### 2.1 Объекты

| Проект | Библиотека/БД | Объект | Роль |
|--------|---------------|--------|------|
| AIS | golden_commission | `объект` | описание |

### 2.2 Данные

| Таблица | Колонки | Описание |
|---------|---------|----------|
| `таблица` | колонка1, колонка2 | описание |

### 2.3 Логика

[Опишите текущую логику работы]

```sql
-- Текущая логика
SELECT ...
FROM ...
WHERE ...
```
```

### 4.3 TO-BE

```markdown
## 3. TO-BE (целевое состояние)

### 3.1 Блок 1: [Название блока]

**Цель:** Описание цели блока.

**Изменения:**

| Объект | Библиотека | Изменение |
|--------|------------|-----------|
| `объект` | golden | описание |

**DDL:**
```sql
-- Изменения таблицы
ALTER TABLE ...
ADD ...
GO
```

**Код:**
```sql
-- Изменения процедуры
...
```

**Риск:** Описание рисков.
```

### 4.4 ЗАТРОНУТЫЕ СИСТЕМЫ

```markdown
## 4. ЗАТРОНУТЫЕ СИСТЕМЫ

| Система | Изменение | Влияние |
|---------|-----------|---------|
| AIS Sybase + PB | да/нет | описание |
| ЛК партнёра | да/нет | описание |
```

### 4.5 РИСКИ

```markdown
## 5. РИСКИ

| Риск | Вероятность | Влияние | Снижение |
|------|-------------|---------|----------|
| описание | низкая/средняя/высокая | низкое/среднее/высокое | описание |
```

### 4.6 КРИТЕРИИ ПРИЁМКИ

```markdown
## 6. КРИТЕРИИ ПРИЁМКИ

### 6.1 Продуктовые критерии

| ID | Критерий |
|----|----------|
| AC-1 | описание |

### 6.2 Технические критерии

| ID | Критерий |
|----|----------|
| TAC-1 | описание |
```

### 4.7 ДЕКОМПОЗИЦИЯ

```markdown
## 7. ДЕКОМПОЗИЦИЯ SYBASE

### 7.1 User Story

| Поле | Значение |
|------|----------|
| Название | Описание |
| Приоритет | High / Medium / Low |
| Оценка | X часов |

### 7.2 Подзадачи

| # | Подзадача | Оценка | Статус |
|---|-----------|--------|--------|
| 1 | Анализ | X ч | [ ] |
| 2 | Разработка | X ч | [ ] |
| 3 | Тестирование | X ч | [ ] |
```

### 4.8 ПРИЛОЖЕНИЯ

```markdown
## 8. ПРИЛОЖЕНИЯ

### 8.1 DDL

```sql
-- Полные DDL-скрипты
```

### 8.2 Код

```sql
-- Полные примеры кода
```

### 8.3 Скриншоты

[Вставьте скриншоты]

### 8.4 Источники

- Ссылка 1
- Ссылка 2
```

---

## 5. Интеграция с инструментами

### 5.1 Поиск объектов

```powershell
# Поиск объекта
ais-objects_search_objects(query="имя объекта")

# Получение информации об объекте
ais-objects_get_object(name="имя объекта", src="current")

# Сравнение объектов
ais-objects_compare_objects(name="имя объекта", kind="PB")
```

### 5.2 Сохранение знаний

```powershell
# Сохранение в ais-catalog (для проекта AIS)
ais-catalog_save_knowledge(text="описание", tags="теги")

# Сохранение в opencode-mcp (для OpenCode)
opencode-mcp_save_knowledge(text="описание", tags="теги")
```

### 5.3 Конвертация в HTML/PDF

```powershell
# Конвертация всех .md файлов в docs/
powershell -NoLogo -File scripts\Convert-Docs.ps1
```

---

## 6. Чек-лист перед утверждением

### 6.1 Содержание
- [ ] Шапка заполнена (задача, статус, дата, платформа, автор)
- [ ] Все 8 секций присутствуют
- [ ] Контекст описан (проблема, связи, источники)
- [ ] AS-IS описан (объекты, данные, логика)
- [ ] TO-BE описан (блоки, DDL, код)
- [ ] Затронутые системы перечислены
- [ ] Риски описаны
- [ ] Критерии приёмки заполнены
- [ ] Декомпозиция создана

### 6.2 Техническое
- [ ] DDL проверены (синтаксис Sybase)
- [ ] Код проверен на совместимость с PS 5.1
- [ ] Ссылки на объекты корректны
- [ ] Таблицы оформлены правильно

### 6.3 Формат
- [ ] Заголовки иерархичны (не более 4 уровней)
- [ ] Код выделен в блоки с указанием языка
- [ ] Таблицы имеют заголовки
- [ ] Списки оформлены правильно

### 6.4 Процесс
- [ ] TQ закрыты (если есть)
- [ ] Документ сохранён в `C:\AIS\AI\Test\Specs\`
- [ ] Документ сконвертирован в HTML/PDF
- [ ] Знания сохранены в MCP

---

## 7. Примеры

### 7.1 Простая задача (1 блок)

```markdown
# Техническая спецификация: Исправление ошибки в процедуре

| Поле | Значение |
|------|----------|
| **Задача** | SYBASE-12345 |
| **Статус** | Черновик |
| **Дата** | 31.08.2026 |
| **Платформа** | AIS (Sybase + PowerBuilder) |
| **Автор** | Иванов И.И. |

---

## 1. КОНТЕКСТ

### 1.1 Проблема/запрос
Ошибка в процедуре `usp_test` при обработке NULL значений.

### 1.2 Связи
- Связанные задачи: SYBASE-12344
- Владелец продукта: Петров П.П.

---

## 2. AS-IS

### 2.1 Объекты

| Проект | Библиотека/БД | Объект | Роль |
|--------|---------------|--------|------|
| DEV_GOLDEN | golden | `usp_test` | Тестовая процедура |

### 2.2 Логика

```sql
-- Текущая логика (ошибка)
IF @param IS NULL
    SET @result = 0  -- Неправильно
```

---

## 3. TO-BE

### 3.1 Блок 1: Исправление NULL-обработки

**Цель:** Корректная обработка NULL значений.

**Изменения:**

| Объект | Библиотека | Изменение |
|--------|------------|-----------|
| `usp_test` | golden | Исправление IF |

**Код:**
```sql
-- Исправленная логика
IF @param IS NULL
    SET @result = NULL  -- Правильно
```

**Риск:** Низкий.
```

### 7.2 Сложная задача (4 блока)

См. пример BSI-9729: `C:\AIS\AI\Test\Specs\techspec-BSI-9729.md`

---

## 8. Частые ошибки

| Ошибка | Решение |
|--------|---------|
| Нет шапки | Всегда начинайте с шапки |
| Неправильный порядок секций | Следуйте порядку 1-8 |
| Код без выделения | Используйте ```sql или ```powershell |
| Таблица без заголовков | Всегда указывайте заголовки |
| Смешанные языки | Russian + English terms (технические термины) |
| Нет DDL для изменений таблиц | Всегда добавляйте ALTER TABLE |
| Нет критериев приёмки | Заполняйте AC и TAC |

---

## 9. Шпаргалка

### Символы

| Символ | Назначение |
|--------|------------|
| `#` | Заголовок |
| `##` | Подзаголовок |
| `###` | Подподзаголовок |
| `-` | Маркированный список |
| `1.` | Нумерованный список |
| `- [ ]` | Чек-лист (не выполнено) |
| `- [x]` | Чек-лист (выполнено) |
| `` ` `` | Код |
| `**` | Жирный текст |
| `*` | Курсив |
| `[текст](url)` | Ссылка |

### Блоки кода

| Язык | Открытие | Закрытие |
|------|----------|----------|
| SQL | ` ```sql ` | ` ``` ` |
| PowerShell | ` ```powershell ` | ` ``` ` |
| DDL | ` ```sql ` | ` ``` ` |
| Текст | ` ``` ` | ` ``` ` |

---

## 10. Дополнительные ресурсы

- Шаблон: `C:\AIS\AI\Test\Specs\techspec-template-ais.md`
- Руководство: `C:\AIS\AI\Test\Specs\techspec-guide-ais.md`
- Пример: `C:\AIS\AI\Test\Specs\techspec-BSI-9729.md`
- Конвертация: `C:\AIS\AI\Prod\scripts\Convert-Docs.ps1`
