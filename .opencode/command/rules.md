---
description: "Открывает WPF-окно со списком всех файлов правил проекта (rules/*.md, AGENTS.md, .opencode/*.mdc). Формат: RULES или ПРАВИЛА."
---

# RULES / ПРАВИЛА

Запусти скрытно (антивирус): `powershell -NoProfile -File scripts\Launch-Hidden.ps1 -Script "Show-Rules.ps1"`. Если скрипта нет — сообщи что файл не найден.