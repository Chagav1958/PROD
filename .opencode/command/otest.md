---
description: Открывает тестовое MSG-окно для проверки оформления диалогов в стиле APPL. Формат: ОТЕСТ. Показывает все элементы: овальные поля, градиентные кнопки, серый градиент фона, тень окна.
---

# ОТЕСТ

Запусти скрытно (антивирус): `powershell -NoProfile -File scripts\Launch-Hidden.ps1 -Script "Show-TestMsg.ps1"`. Если скрипта нет — сообщи что файл не найден.
