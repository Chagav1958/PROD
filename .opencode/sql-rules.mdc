---
description: Sybase ASE 15.5 SQL — синтаксис, функции, особенности сервера
globs: **/*.sql
alwaysApply: false
---

# SQL правила для Sybase ASE 15.5

Сервер базы данных: **Sybase Adaptive Server Enterprise (ASE) 15.5** (СУБД проекта AIS). Язык SQL этого сервера имеет ряд отличий от других СУБД (MS SQL, PostgreSQL, Oracle). Ниже приведены ключевые особенности, актуальные для данного репозитория.

## ПРАВИЛО 0 (КРИТИЧНО): Читай MCP ПЕРЕД написанием SQL

**ОБЯЗАТЕЛЬНО** перед написанием ЛЮБОГО SQL-кода (даже простого SELECT):

1. **`sybase-docs_search_docs`** — найти документацию по функциям/конструкциям
2. **`sybase-docs_validate_code(product="ase15.5")`** — проверить синтаксис
3. **НЕ полагаться на память** — синтаксис ASE отличается от T-SQL, PostgreSQL, Oracle

**Типичные ошибки из-за памяти:**
- `i.rowcnt` — такой колонки НЕТ в `sysindexes` ASE (есть в MS SQL)
- `data_pgs()`, `index_pgs()` — не документированы в ASE 15.5
- `reserved_pgs()` — документирована, работает
- `DATALENGTH()` — проверить через validate_code
- `CONVERT(char(10), date, 120)` — стиль 120 может не поддерживаться в ASE

**Последовательность действий:**
1. Запросить документацию: `sybase-docs_search_docs(query="sysindexes columns ASE")`
2. Проверить функции: `sybase-docs_validate_code(code="...", product="ase15.5")`
3. Если есть предупреждения — исправить код
4. Только после успешной валидации — записывать файл

## Строковые функции

### STR_REPLACE — вместо REPLACE

Для замены подстроки используется `STR_REPLACE()`, а не стандартный `REPLACE()`:

```sql
-- ПРАВИЛЬНО
SET @tmp_sql = STR_REPLACE(@tmp_sql, 'and p.state <>', 'and p.state =')

-- НЕПРАВИЛЬНО
SET @tmp_sql = REPLACE(@tmp_sql, 'and p.state <>', 'and p.state =')
```

### CHARINDEX — поиск подстроки

Поддерживается только 2-аргументная форма (без начальной позиции):

```sql
SELECT @pos = CHARINDEX(',', @insure_kind)
IF @pos > 0
    SELECT @item = SUBSTRING(@insure_kind, 1, @pos - 1)
```

> В отличие от MS SQL Server, `CHARINDEX` в ASE 15.5 **не поддерживает** третий аргумент (start_location). Для поиска с позиции нужно использовать `SUBSTRING` + `CHARINDEX`.

### CHAR_LENGTH — длина строки

Используется `CHAR_LENGTH()`, не `LEN()` и не `LENGTH()`:

```sql
IF CHAR_LENGTH(@insure_kind) > 0
```

### SUBSTRING — 1-based индексация

```sql
SUBSTRING(p.date_sale, 9, 2)  -- символы с 9-й позиции, 2 символа
```

### STUFF — удаление/замена по позиции

```sql
SELECT @insurant = STUFF(@insurant, CHARINDEX(CHAR(13), @insurant), 1, NULL)
```

### LTRIM / RTRIM — обрезка пробелов

```sql
SELECT @val = LTRIM(RTRIM(@val))
```

### CHAR() — специальные символы

```sql
CHAR(13)   -- возврат каретки (CR)
CHAR(10)   -- перевод строки (LF)
CHAR(9)    -- табуляция (TAB)
```

## Функции даты и времени

### КРИТИЧНО: формат даты в SQL-литералах — только ISO `YYYY-MM-DD`

**На серверах ASE проекта (в т.ч. galaxy) действует формат даты `mdy`** (месяц-день-год).
Строка `'07.09.2026'` интерпретируется как **9 июля 2026**, а НЕ 7 сентября!
Это приводит к ложным результатам сравнения дат и к неверным выводам в диагностике.

**Правило:** все даты в SQL-литералах указывать ТОЛЬКО в формате ISO `YYYY-MM-DD`:
```sql
-- ПРАВИЛЬНО (однозначно, не зависит от dateformat сервера)
WHERE d.date_from <= '2026-09-07'
AND IsNull(d.date_cancel, d.date_to) >= '2026-09-07'

-- НЕПРАВИЛЬНО (на galaxy с dateformat mdy читается как 9 июля)
WHERE d.date_from <= '07.09.2026'
```

Проверка интерпретации:
```sql
-- 104 = dd.mm.yyyy. Ожидается: ISO -> 07.09.2026; '07.09.2026' -> 09.07.2026
SELECT convert(varchar(30), convert(datetime, '2026-09-07'), 104),  -- 07.09.2026 (верно)
       convert(varchar(30), convert(datetime, '07.09.2026'), 104)   -- 09.07.2026 (mdy!)
```

**Опыт (16.09.2026, SUPRT-19100):** тест на galaxy с `'07.09.2026 00:00:00'` дал
положительный ID просроченной доверенности (190028527), т.к. дата прочитана как
9 июля — доверенность тогда ещё действовала. С ISO `'2026-09-07'` функция вернула
минус-ID `-190028527` (корректно). Знание хранилось только в файлах задачи и не
попало в глобальные правила — из-за этого ошибка повторилась.

### Текущая дата/время

```sql
SET @date_now      = current_date()   -- только дата (time part 00:00:00)
SET @date_time_now = GetDate()        -- дата + время
```

### DATEADD — добавление интервала

```sql
SET @new_date = DATEADD(dd, @days, @date)       -- дни
SET @new_date = DATEADD(yy, -3, @date)          -- года (отрицательный сдвиг)
SET @new_date = DATEADD(dy, 1, @date)           -- день года (синоним dd)
SET @new_date = DATEADD(ms, 500, @datetime)     -- миллисекунды
```

### DATEDIFF — разница между датами

```sql
SELECT @diff_ms = DATEDIFF(ms, @start_dt, @finish_dt)
```

### DATEPART — извлечение части даты

```sql
SET @day_number = DATEPART(dw, @today)     -- день недели
SELECT @year    = DATEPART(yy, a.date_out) -- год
```

### CONVERT — форматирование даты

Стили форматирования (третий аргумент):

| Стиль | Формат | Пример |
|---|---|---|
| `23` | `yyyy-mm-dd` | `2024-01-15` |
| `102` | `yyyy.mm.dd` | `2024.01.15` |
| `105` | `dd-mm-yyyy` | `15-01-2024` |
| `111` | `yyyy/mm/dd` | `2024/01/15` |

```sql
CONVERT(VARCHAR(10), @date, 111)   -- '2024/01/15'
CONVERT(VARCHAR(10), @date, 105)   -- '15-01-2024'
CONVERT(DATE, @date_string)        -- приведение строки к DATE
```

## CASE — условное выражение

Поддерживается в двух формах:

```sql
-- Простая форма: CASE поле WHEN значение THEN результат
SELECT CASE a.state
    WHEN 0 THEN 'Новый'
    WHEN 1 THEN 'Активен'
    WHEN 2 THEN 'Закрыт'
    ELSE 'Неизвестно'
END

-- Расширенная форма: CASE WHEN условие THEN результат
SELECT CASE
    WHEN CHARINDEX('-', a.num_dover) = 0 THEN CHAR_LENGTH(a.num_dover) - 2
    ELSE CHARINDEX('-', a.num_dover) - 1
END
```

## Ограничение количества строк

### SELECT TOP N

```sql
SELECT TOP 1 policy_id, num_agreement FROM oss_agreement
```

### SET ROWCOUNT N

```sql
SET ROWCOUNT @rows     -- ограничить количество строк
SELECT ... FROM ...
SET ROWCOUNT 0         -- сбросить (обязательно!)
```

> `LIMIT` и `OFFSET` не поддерживаются.

## Обработка NULL

### ISNULL — замена NULL на значение (основной способ)

```sql
SELECT ISNULL(@param, 'default')
SELECT ISNULL(date_cancel, date_to)
```

### COALESCE — первый не-NULL из списка (используется реже)

```sql
SELECT COALESCE(date_cancel, date_to, '2099-01-01')
```

> `NULLIF` в проекте не используется.

## Временные таблицы

- Создаются через `SELECT ... INTO #temp` или `CREATE TABLE #temp (...)`
- Префикс `#` обязателен:

```sql
CREATE TABLE #tmp_pol (
    os_policy_id NUMERIC(10,0),
    system       VARCHAR(10) NULL,
    ais_policy_id NUMERIC(10,0) NULL
)

SELECT ... INTO #tmp_paym FROM paym_golden

DROP TABLE #tmp_pol
```

## Идентификаторы (IDENTITY / @@IDENTITY)

- Автоинкрементные колонки: `IDENTITY` (не `AUTO_INCREMENT`)
- Получение последнего ID: `@@IDENTITY` (не `LAST_INSERT_ID()`)

```sql
INSERT INTO fin_trans (...) VALUES (...)
SELECT @err = @@ERROR, @fin_trans_id = @@IDENTITY

-- Создание таблицы с identity-колонкой
CREATE TABLE #result (
    row_number NUMERIC(10,0) IDENTITY,
    field_name VARCHAR(255) NULL
)
```

### SET IDENTITY_INSERT — явная вставка в identity-колонку

```sql
SET IDENTITY_INSERT table_name ON
INSERT INTO table_name (identity_col, ...) VALUES (значение, ...)
SET IDENTITY_INSERT table_name OFF
```

## Конкатенация строк

```sql
SET @err_text = 'Ошибка при обработке ID = ' + CONVERT(VARCHAR(10), @ag_id)
```

## Транзакции

```sql
BEGIN TRAN
    INSERT INTO period_journal (...) VALUES (...)
    IF @@ERROR <> 0 BEGIN
        ROLLBACK TRAN
        RETURN -1
    END
COMMIT TRAN

-- Гарантированный откат при ошибке
IF @@ERROR <> 0 BEGIN
    WHILE (@@TRANCOUNT > 0) BEGIN
        ROLLBACK TRAN
    END
END
```

> `SAVE TRAN` (сохранённые точки) в проекте не используются.

## Обработка ошибок

### @@ERROR и @@ROWCOUNT — всегда парой

```sql
SELECT @RC = @@ERROR, @row_count = @@ROWCOUNT
IF @RC <> 0 BEGIN
    GOTO EXIT_PROC
END
```

### RAISERROR

Два варианта синтаксиса (оба используются в проекте):

```sql
-- Вариант 1: пробел между номером и сообщением
RAISERROR 99999 'Описание ошибки'

-- Вариант 2: запятая между номером и сообщением
RAISERROR 99999, 'Описание ошибки'

-- Сообщение с подстановкой переменной
RAISERROR 99999 'Ошибка обновления записи %1!', @entity_id
RETURN -100
```

### GOTO — переход на метку

```sql
EXIT_PROC:
    PRINT 'Завершение.'
    RETURN @RC
```

### PRINT — вывод отладочных сообщений

`PRINT` поддерживает форматирование с плейсхолдерами `%N!`:

```sql
PRINT '@RC = %1!, @err_text = %2!', @RC, @err_text
PRINT 'Count = %1!', @Count_com
```

## Динамический SQL

```sql
SELECT @tmp_sql = 'SELECT ... WHERE id = ' + CONVERT(VARCHAR(15), @id)
EXECUTE (@tmp_sql)
```

## Системные переменные

| Переменная | Назначение |
|---|---|
| `@@IDENTITY` | Последний вставленный identity |
| `@@ERROR` | Код последней ошибки |
| `@@ROWCOUNT` | Количество строк, затронутых последней операцией |
| `@@TRANCOUNT` | Глубина вложенности транзакций |
| `@@SPID` | ID текущего процесса (spid) |
| `@@PROCID` | ID текущей процедуры |
| `@@SQLSTATUS` | Статус последней операции FETCH (0 = успешно) |

## Хранимые процедуры

```sql
CREATE PROCEDURE dbo.usp_name;1
    @param1 NUMERIC(10,0),
    @param2 INT = 0,           -- параметр по умолчанию
    @param3 VARCHAR(255) OUTPUT -- выходной параметр
AS
BEGIN
    ...
    RETURN @RC
END

-- WITH RECOMPILE
CREATE PROCEDURE dbo.usp_name WITH RECOMPILE
AS
BEGIN
    ...
END
```

> Суффикс `;1` после имени процедуры — стандартное соглашение Sybase ASE (номер версии).

## Межбазовые запросы

```sql
SELECT ... FROM golden.dbo.paym_golden
SELECT ... FROM master..sysprocesses
```

## Курсоры

```sql
DECLARE cur_name CURSOR FOR SELECT ... FROM ...
OPEN cur_name
FETCH cur_name INTO @var1, @var2
WHILE (@@SQLSTATUS = 0) BEGIN
    ...
    FETCH cur_name INTO @var1, @var2
END
CLOSE cur_name
DEALLOCATE CURSOR cur_name
```

## Подсказки (Hints)

### Индексные хинты

Указываются в скобках после имени таблицы:

```sql
SELECT ... FROM ais_policy p (INDEX os_policy_id)
SELECT ... FROM oss_agreement (INDEX ix_agreement_date)
```

### Блокировки

```sql
LOCK TABLE table_name IN EXCLUSIVE MODE
LOCK TABLE table_name IN SHARED MODE
-- WAIT — таймаут ожидания блокировки (секунды):
LOCK TABLE ais_job_status IN EXCLUSIVE MODE WAIT 1
```

## Системные функции

| Функция | Назначение |
|---|---|
| `SUSER_NAME()` | Имя текущего пользователя сервера |
| `HOST_NAME()` | Имя хоста текущего подключения |
| `PROGRAM_NAME()` | Имя клиентской программы (из `master..sysprocesses.program_name`) |
| `OBJECT_NAME(@@PROCID)` | Имя текущей хранимой процедуры |

## Чего НЕТ в Sybase ASE 15.5 (важно для мигрантов с MS SQL)

| Конструкция | Чем заменять |
|---|---|
| `DECLARE @table TABLE (...)` | Временная таблица `#table` через `CREATE TABLE #table` |
| `OUTPUT` (INSERT/UPDATE/DELETE) | После операции — `SELECT @@IDENTITY`, `@@ROWCOUNT` |
| `MERGE` | Отдельные `INSERT`/`UPDATE`/`DELETE` с проверкой `IF EXISTS` |
| `TRY...CATCH` | `@@ERROR` + `GOTO` + метка (см. раздел «Обработка ошибок») |
| `LIMIT` / `OFFSET` | `SELECT TOP N` или `SET ROWCOUNT N` |
| `AUTO_INCREMENT` | `IDENTITY` (см. выше) |
| `LAST_INSERT_ID()` | `@@IDENTITY` |
| `ROW_NUMBER() OVER (...)` | **Поддерживается**, но в проекте не используется |
| CTE (`WITH ... AS`) | **Поддерживается**, но в проекте не используется |
| `TRUNCATE TABLE` | **Поддерживается** (но без логирования только при определённых настройках БД) |
| `EXCEPT` / `INTERSECT` | Не поддерживаются. Заменять через `NOT EXISTS` / `INNER JOIN` |

## Особенности кодировки

- Датампы SQL-объектов в папке `BD` сохранены в кодировке **Windows-1251** (cp1251). При просмотре или редактировании этих файлов убедитесь, что редактор использует правильную кодировку.
- Кодировка строковых данных на сервере может отличаться от кодировки файлов экспорта. При сравнении кириллических строк через `COLLATE` используйте collation, соответствующее базе данных.

## Правила оформления SQL-кода

- Имена таблиц: `dbo.имя_таблицы` (префикс схемы).
- Названия таблиц и полей — в `snake_case`.
- Псевдонимы таблиц — короткие (1–3 буквы): `p` для policy, `a` для agreement, `e` для entity.
- `SELECT` — ключевые слова заглавными.
- Каждый `SELECT` на новой строке, поля с отступом.

## Квалификаторы колонок и dbo. — ошибка 107 (ОБЯЗАТЕЛЬНО)

ASE сопоставляет квалификатор колонки и имя таблицы во FROM **как строки** — они должны совпадать текстуально (ASE 15.5, blocks.pdf стр. 399 «Using qualified identifiers consistently»).

**Правило:** если во FROM/JOIN таблица указана с владельцем (`dbo.таблица`) — ОБЯЗАТЕЛЬНО ввести алиас и использовать его во всех префиксах колонок (SELECT/WHERE/ON/GROUP/ORDER). Добавлять `dbo.` в префиксы колонок — плохая идея (громоздко, легко пропустить).

```sql
-- ПРАВИЛЬНО (алиас)
SELECT p.product_id, p.name
FROM dbo.ais_cat_product p
LEFT JOIN dbo.ais_cat_prolongation_type pt ON p.prolongation_type_id = pt.prolongation_type_id

-- НЕПРАВИЛЬНО (рассинхрон: FROM с dbo., префиксы без dbo.)
SELECT ais_cat_product.product_id, ais_cat_product.name
FROM dbo.ais_cat_product
```

**Коварство бага — user-зависимость:** у пользователя, который в БД маппится на `dbo` (например, разработчик), префикс `таблица.` резолвится в `dbo.таблица` и совпадает — ошибки НЕТ. У обычного пользователя (не dbo) префикс резолвится через его имя, НЕ совпадает со строкой `dbo.таблица` → **ошибка 107** («The column prefix ... does not match with a table name or alias name used in the query»). Поэтому у автора правки всё работает, а у других пользователей падает.

Правило распространяется и на SQL внутри PB DataWindow/DDDW (retrieve). Прецедент: `dddw_ais_cat_product`, `d_ais_cat_product` в карточке договора с посредником (знания ais-catalog #204-206).

## Различия ASA SQL и T-SQL (MS SQL) — ОБЯЗАТЕЛЬНО

**Критическое правило:** Sybase ASE 15.5 — НЕ MS SQL Server. Перед написанием SQL-скриптов ОБЯЗАТЕЛЬНО:
1. Читать документацию через `sybase-docs_search_docs`
2. Проверять синтаксис через `sybase-docs_validate_code(product="ase15.5")`
3. Не использовать T-SQL-специфичные конструкции

### Таблица соответствий (ASA SQL vs T-SQL)

| Конструкция | ASA SQL (Sybase) | T-SQL (MS SQL) | Комментарий |
|-------------|------------------|----------------|-------------|
| Имя пользователя | `SUSER_NAME()` | `USER_NAME()` или `SUSER_SNAME()` | В ASE `USER_NAME()` возвращает имя пользователя БД, не сервера |
| Разделитель пакетов | `go` | `GO` или `;` | В ASE `go` обязателен между пакетами |
| Дата начала/окончания (ais_entity_basis) | `date_from`, `date_to` | `date_begin`, `date_end` | В проекте AIS используется `date_from`/`date_to` |
| Полисы AVT/ОСАГО | `oss_agreement` | `ais_policy` | Для AVT-полисов (еОСАГО) используется `oss_agreement`, не `ais_policy` |
| Посредники по полисам | `oss_middleman` (колонки: `mid_name_id`, `business_role`, `mid_name`) | — | В `oss_middleman` НЕТ колонки `subj_id`; для связи с посредником используется `mid_name_id` |
| Транзакции | `BEGIN TRAN` / `COMMIT TRAN` / `ROLLBACK TRAN` | `BEGIN TRANSACTION` / `COMMIT` / `ROLLBACK` | В ASE поддерживается сокращённая форма `TRAN` |

### Примеры ошибок (SUPRT-19116, 10.09.2026)

**Ошибка 1:** Использование `USER_NAME()` вместо `SUSER_NAME()`

```sql
-- НЕПРАВИЛЬНО (T-SQL синтаксис)
UPDATE dbo.ais_commission
SET update_user = USER_NAME()
WHERE com_id = 12345

-- ПРАВИЛЬНО (ASA SQL)
UPDATE dbo.ais_commission
SET update_user = SUSER_NAME()
WHERE com_id = 12345
```

**Ошибка 2:** Использование `date_begin`/`date_end` вместо `date_from`/`date_to`

```sql
-- НЕПРАВИЛЬНО (колонки не существуют в ais_entity_basis)
SELECT mis_ag_id, date_begin, date_end
FROM dbo.ais_entity_basis
WHERE ag_id = 484460

-- ПРАВИЛЬНО
SELECT mis_ag_id, date_from, date_to
FROM dbo.ais_entity_basis
WHERE ag_id = 484460
```

**Ошибка 3:** Использование `ais_policy` для AVT-полисов

```sql
-- НЕПРАВИЛЬНО (для AVT/еОСАГО используется oss_agreement)
SELECT os_policy_id, num_agreement, system
FROM dbo.ais_policy
WHERE num_agreement IN ('XXX0668448907') AND flag_not_actual = 0

-- ПРАВИЛЬНО (AVT-полисы в oss_agreement)
SELECT oss_policy_id, num_agreement, blocked
FROM dbo.oss_agreement
WHERE num_agreement IN ('XXX0668448907')
```

**Ошибка 4:** Использование `subj_id` в `oss_middleman`

```sql
-- НЕПРАВИЛЬНО (колонки subj_id нет в oss_middleman)
SELECT os_policy_id, subj_id, mid_name_id
FROM dbo.oss_middleman

-- ПРАВИЛЬНО (используется mid_name_id для связи с посредником)
SELECT os_policy_id, mid_name_id, business_role, mid_name
FROM dbo.oss_middleman
```

### Правило проверки перед написанием SQL

ПЕРЕД записью любого `.sql` файла или SQL-фрагмента:

1. **Проверить имена колонок** через `SELECT name FROM syscolumns WHERE id = object_id('имя_таблицы') ORDER BY colid`
2. **Валидировать синтаксис** через `sybase-docs_validate_code(code="...", product="ase15.5")`
3. **Проверить документацию** через `sybase-docs_search_docs(query="имя_функции или конструкция")`

Не полагаться на память — синтаксис ASE отличается от T-SQL, PostgreSQL, Oracle.

## Безопасность данных — ЗАПРЕЩЕНО выводить конфиденциальную информацию

**Строгое правило:** При диагностических запросах и отладочных скриптах **НЕ выводить** данные, которые могут привести к идентификации клиента или раскрытию финансовой информации.

### Запрещено выводить (в SELECT/PRINT/диагностике):

| Категория | Примеры колонок | Риск |
|-----------|-----------------|------|
| **Имена клиентов/посредников** | `entity_name`, `mid_name`, `insurant`, `per_last_name`, `per_first_name` | Идентификация лица |
| **Номера полисов/договоров** | `num_agreement`, `policy_number` | Идентификация договора |
| **Суммы/финансы** | `com_sum`, `pp_sum`, `premium`, `paym_sum` | Финансовая информация |
| **Адреса/телефоны** | `address`, `phone`, `email` | Персональные данные |
| **Номера счетов** | `account_number`, `bank_account` | Банковская информация |

### Разрешено выводить (для диагностики):

| Категория | Примеры колонок | Назначение |
|-----------|-----------------|------------|
| **Внутренние ID** | `com_id`, `entity_id`, `ag_id`, `os_policy_id`, `mis_ag_id` | Связи между объектами |
| **Коды/статусы** | `com_type`, `system`, `state`, `business_role`, `blocked` | Логика работы |
| **Ссылки** | `cover_id`, `mid_name_id`, `dep_id` | Структура данных |
| **Даты (без привязки к клиенту)** | `date_from`, `date_to`, `paym_date` | Временные рамки |
| **Технические поля** | `update_foundation`, `update_user`, `check_info` | Отладка |

### Примеры:

```sql
-- НЕПРАВИЛЬНО (выводит конфиденциальные данные)
SELECT c.com_id, e.entity_name, c.com_sum, p.num_agreement
FROM dbo.ais_commission c
JOIN dbo.ais_entity_data e ON e.entity_id = c.entity_id
JOIN dbo.oss_agreement p ON p.oss_policy_id = c.os_policy_id

-- ПРАВИЛЬНО (только внутренние ID и коды)
SELECT c.com_id, c.entity_id, c.ag_id, c.com_type, c.system, c.state, c.os_policy_id
FROM dbo.ais_commission c
WHERE c.com_id IN (186262303, 187671845)

-- НЕПРАВИЛЬНО (выводит имя посредника)
SELECT os_policy_id, mid_name, subj_id
FROM dbo.oss_middleman

-- ПРАВИЛЬНО (только ID и роли)
SELECT os_policy_id, mid_name_id, business_role, cover_id
FROM dbo.oss_middleman
WHERE os_policy_id IN (139477263, 140633041)
```

### Правило для диагностических скриптов:

1. **Проверять SELECT-список** перед выполнением — нет ли конфиденциальных полей
2. **Использовать только ID** для связи между таблицами
3. **Если нужны имена** — выполнять запрос локально, не сохранять в файлы/логи
4. **При сохранении в MCP/файлы** — только ID и коды, без персональных данных

Это правило защищает клиентов при возможной утечке диагностических данных наружу.
